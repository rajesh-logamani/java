package H_collections.collectionsExercise;

import java.util.HashSet;

public class HashSetExercise {

    public static void main(String[] args) {
        // Hashtable
        //

        // HashSet
        // A HashSet is a collection that contains no duplicate elements and has no defined order.
        // It is backed by a hash table, which allows for fast retrieval of elements.
        System.out.println("HashSet Example:");
        HashSet<Integer> hashSet = new HashSet<>();
        // Adding elements to the HashSet
        hashSet.add(10);
        hashSet.add(20);
        hashSet.add(30);
        hashSet.add(40);
        hashSet.add(30);

        System.out.println("Initial HashSet: " + hashSet); // Output: [10, 20, 30, 40]

        // HashMap
        // HashAttributeSet
        // HashDocAttributeSet
        //  HashPrintJobAttributeSet
        // HashPrintRequestAttributeSet
        //HashPrintServiceAttributeSet
    }
}
